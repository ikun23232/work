<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kgc.dao.UserDao">
    <insert id="addUser">
        insert into easybuy_user(loginName, userName, password, sex, identityCode, email, mobile, type)
        values (#{loginName}, #{loginName}, #{password}, #{sex}, #{identityCode}, #{email}, #{mobile}, #{type})
    </insert>
    <select id="checkUserByName" resultType="User">
        select id,
               loginName,
               userName,
               password,
               sex,
               identityCode,
               email,
               mobile,
               type
        from easybuy_user
        where loginName = #{loginName}
    </select>

</mapper>